---
date: 2026-07-26
category: 주간종합
subject: Cole Medin 메모리 시스템 재조명 — 무엇을 기억하게 할 것인가의 분류학
tags: [AI탐구, deepdive]
starred: false
micro_action: 비서앱 레포 CLAUDE.md 열어서 "자주 반복하는 실수 top 3"를 직접 손으로 한 줄씩 적어 시드 메모리 심기
---

# Cole Medin 메모리 시스템 재조명 — 무엇을 기억하게 할 것인가의 분류학

## 누구/무엇인가

7월 24일 deepdive에서 Cole Medin의 **claude-memory-compiler**를 소개했습니다. 메커니즘을 요약하면: Claude Code 세션이 끝날 때 대화 로그를 추출해 구조화된 위키로 컴파일하고, 다음 세션 시작 시 자동으로 주입하는 자기진화 메모리 시스템입니다. GitHub 스타 1,300개·포크 314개, 공개 48시간 내 237스타. 기술적 구조는 세 훅(SessionStart·PreCompact·SessionEnd)과 flush.py·compile.py 두 스크립트입니다.

그런데 그 deepdive에서 다루지 않은 질문이 있었습니다. **"그래서 무엇을 기억하게 해야 하는가?"** 훅을 설치하는 방법은 알았습니다. 하지만 Claude가 쌓아야 할 메모리의 **내용 분류 체계**에 대해서는 깊이 들어가지 않았습니다. 오늘은 그 안쪽으로 들어갑니다. 솔로 운영자가 비서앱과 ERP를 동시에 개발하고 운영하는 맥락에서, 메모리에 무엇을 담을 것인지의 분류학입니다.

## 무엇이 특별한가

### 1. 기억의 계층 — 핫·웜·콜드 메모리

모든 정보가 동등하게 기억할 가치를 갖지 않습니다. Cole의 시스템이 작동하려면 먼저 "무엇을 추출하게 할 것인가"의 분류가 선행되어야 합니다. 대화 로그에서 flush.py가 추출할 항목을 직접 정의할 수 있기 때문입니다.

실무에서 유용한 세 계층을 제안합니다:

**핫메모리(Hot Memory)** — 이번 작업 세션 내 컨텍스트. "지금 결제 모듈 리팩토링 중이고 다음 세션도 이어서 진행"처럼 몇 시간~며칠 안에 소비됩니다. CLAUDE.md의 현재 작업 상태(Current Work) 섹션이 이 역할을 합니다. 짧고 구체적이어야 하며, 완료되면 지웁니다.

**웜메모리(Warm Memory)** — 이번 달·분기 결정 사항. "ERP 주문 상태 컬럼은 문자열이 아닌 enum으로 통일하기로 함(2026-07-15)"처럼 몇 주~몇 달간 유효한 설계 결정들. claude-memory-compiler가 가장 잘 쌓는 층위입니다. 결정의 **이유**가 함께 담겨야 가치가 있습니다.

**콜드메모리(Cold Memory)** — 아키텍처 원칙과 도메인 지식. "와당탕은 KST 기준 날짜 처리만 한다", "느린호밀 주문서는 항상 CSV 파싱 전 BOM 제거"처럼 코드베이스 전체에 적용되는 불변 규칙들. 한번 정립되면 거의 바뀌지 않습니다. Obsidian vault의 Evergreen Note에 해당합니다.

### 2. 기억해야 할 것 vs 잊어도 될 것

이것이 실제로 어렵습니다. flush.py가 모든 대화를 통째로 추출하면 위키가 잡음으로 가득 차고, 너무 엄격히 필터링하면 중요한 결정이 증발합니다. Cole 본인이 언급한 기준은 **"미래의 나에게 유용한가"**입니다. 더 구체적으로 분해하면:

기억해야 할 것들: ① 반복된 실수와 그 원인("UTC 버그 3번 — KST 변환 빠트렸을 때"), ② 포기한 대안과 이유("Redis 캐시 도입 검토했으나 단일 서버 배포라 오버킬"), ③ 외부 API의 특이 동작("카카오페이 웹훅은 항상 retry가 있어서 멱등성 보장 필수"), ④ 코드베이스 규약("컴포넌트명은 한국어 도메인어 영문 표기, 예: DailySalesReport").

잊어도 될 것들: ① "이 코드 어떻게 작동하는지 설명해줘" 같은 탐색 대화, ② 이미 close된 작업의 구현 상세, ③ Claude가 틀린 제안을 했다가 수정한 중간 과정, ④ 단순 문법 오류 수정.

이 기준을 flush.py 프롬프트에 직접 명시합니다. *"Extract only: (1) architectural decisions with reasons, (2) recurring bugs and their root causes, (3) external API quirks specific to this codebase, (4) conventions we decided to follow. Skip: exploratory chats, closed tasks, syntax fixes."*

### 3. 비서앱과 ERP — 두 레포의 메모리 전략

비서앱과 ERP는 서로 다른 도메인을 다루기 때문에 메모리도 분리가 원칙입니다. 하지만 공유 메모리가 있습니다. 이 구분이 실무에서 중요합니다.

**분리해야 할 메모리**: 비서앱의 텔레그램 봇 명령 체계, ERP의 DB 스키마 결정, 각각의 외부 API 연동 특이사항. 이건 레포별 위키로 쌓습니다.

**공유 메모리**: 대표님 개인 코딩 선호도("나는 async/await 패턴을 일관되게 씀"), 와당탕 도메인 용어 정의("공구 = 공동구매, 1~3일 한정"), 전체에 적용되는 보안 원칙("API 키는 절대 코드에 직접 안 씀"). 이건 홈 디렉터리 또는 공통 레포의 CLAUDE.md에 담습니다.

Cole의 시스템에서는 SessionStart 훅이 어떤 위키 인덱스를 주입할지 레포마다 다르게 설정할 수 있습니다. 비서앱 레포에서 Claude를 시작하면 비서앱 위키 + 공통 원칙이 주입되고, ERP 레포에서 시작하면 ERP 위키 + 공통 원칙이 주입됩니다.

### 4. 메모리 유효기간과 정리 루틴

메모리에는 유통기한이 있습니다. "주문 상태 enum 추가하기로 함"이라는 결정이 구현 완료된 후에도 위키에 남아 있으면 Claude가 이미 완료된 TODO처럼 처리할 수 있습니다. Cole의 시스템이 자동 컴파일을 제공하지만, 정기적인 수동 정리 루틴이 필요합니다.

제안하는 루틴: **주 1회(15분) 위키 리뷰**. 지난 주 추가된 아티클 중 이미 구현 완료된 결정은 "Completed" 태그 추가, 더 이상 유효하지 않은 결정은 삭제 또는 "Deprecated" 표시. 이 루틴 자체를 비서앱에서 매주 금요일 알림으로 만들면 자동화가 됩니다.

### 5. Obsidian Second Brain과의 연결점

cole-memory-compiler가 쌓는 위키는 **코드베이스 전용 지식**입니다. 대표님의 Obsidian vault가 담는 것과는 다릅니다. 전략적 결정("왜 이 비즈니스를 하는가"), 학습 메모(이 deepdive 시리즈), 개인 루틴은 Obsidian에 있는 것이 맞습니다. claude-memory-compiler의 위키는 **코드 실행 현장에서 축적되는 암묵지** — Obsidian의 Evergreen 노트가 개인 사고의 두 번째 뇌라면, 이건 **코드베이스의 두 번째 뇌**입니다.

둘을 연결하는 방법: Obsidian 볼트의 "Project/비서앱" 폴더에 claude-memory-compiler 위키의 핵심 아티클을 주 1회 복사합니다. 방향은 단방향(코드 위키 → Obsidian)이어야 합니다. Obsidian에서 직접 편집하면 코드 레포 위키와 동기화 불일치가 생깁니다.

## 와당탕/느린호밀 적용 포인트

**오늘 30분 (micro)**: 비서앱 레포의 CLAUDE.md를 열고 맨 아래에 `## 반복 실수 & 도메인 규약` 섹션을 추가합니다. 직접 손으로 지금까지 세 번 이상 반복한 실수, 도메인 용어 정의, 외부 API 특이사항을 한 줄씩 씁니다. 이것이 메모리 시스템의 씨앗 데이터(seed data)입니다. 자동화보다 이 씨앗 데이터 품질이 첫 한 달의 메모리 품질을 결정합니다.

```markdown
## 반복 실수 & 도메인 규약

- 날짜는 항상 KST 기준 처리. datetime.now()는 절대 사용 금지, 반드시 datetime.now(KST)
- 카카오페이 웹훅: 동일 결제에 중복 전송 있음 → idempotency key 필수
- 공구(공동구매) 주문서: CSV 파싱 전 BOM(UTF-8 with BOM) 제거 선행
- 와당탕 주문 상태: pending / confirmed / shipped / delivered / cancelled (이 5가지 외 추가 금지)
```

**이번 주 1-2시간 (mid)**: claude-memory-compiler를 비서앱 레포에 설치하고, 위에서 작성한 씨앗 데이터를 초기 위키 아티클로 변환합니다. 3개 주제로 나눕니다: (1) 날짜·시간 처리 규약, (2) 외부 API 특이사항, (3) 도메인 용어 사전. 각 아티클이 5~10줄 이내로 압축되어야 합니다. 길면 길수록 Claude가 주입 시 핵심을 놓칩니다.

```python
# 위키 아티클 예시 — 간결함이 핵심
# wiki/datetime-conventions.md
"""
# 날짜·시간 처리 규약

**규칙**: 모든 datetime은 KST 기준.
**이유**: 와당탕 주문 마감 시간이 한국 기준이며, UTC 혼용 시 당일 마감 계산 버그 발생 (2회 경험).
**코드**: from zoneinfo import ZoneInfo; KST = ZoneInfo('Asia/Seoul')
**금지**: datetime.now() 직접 호출
"""
```

**이번 달 실험 (macro)**: 한 달 후 Claude에게 컨텍스트 없이 "비서앱에서 날짜 처리할 때 주의할 게 뭐가 있어?"라고 물어봅니다. 메모리 위키가 없는 상태의 답변과 비교합니다. 측정 지표: ① 정확한 KST 규약 언급 여부, ② 이전 버그 사례 인지 여부, ③ 추가 설명 없이 올바른 코드 스니펫 생성 여부.

## 한국 솔로 운영자 맥락에서 주의

**메모리를 너무 많이 쌓으면 위험합니다.** 모든 결정을 기록하려는 유혹을 경계해야 합니다. 위키 아티클이 200개를 넘으면 SessionStart 주입 토큰 비용이 커지고, 더 심각하게는 Claude가 어떤 아티클이 현재 작업과 관련 있는지 판단하는 데 인지 비용을 씁니다. 과적재된 메모리는 메모리가 없는 것보다 나쁠 수 있습니다. "이 정보가 없으면 Claude가 같은 실수를 반복하는가?"가 기록 결정의 유일한 기준이어야 합니다.

**자동 추출을 맹신하지 않기**. flush.py가 "저장할 가치 있는 것"을 추출하지만, LLM이 판단하는 "중요도"와 대표님이 실제로 원하는 "중요도"는 다릅니다. 초기 2주는 매일 flush.py 출력을 검토하고, 잘못 캡처된 것은 삭제, 빠진 결정은 수동 추가하는 루틴을 지켜야 합니다. 이 초기 캘리브레이션이 메모리 품질의 기반입니다.

## 더 깊이 보려면

- [coleam00/claude-memory-compiler GitHub](https://github.com/coleam00/claude-memory-compiler)
- [7월 24일 deepdive: Claude Code 자기진화 메모리 설계 (메커니즘 심층)](https://github.com/hi62520/hyein-ai-deepdive/blob/main/2026-07-24_deepdive_에이전트빌딩_Cole-Medin-자기진화메모리-두번째뇌.md)
- [Andrej Karpathy — LLM Knowledge Base 원문 (1.7M 뷰)](https://x.com/karpathy/status/1805104312756830701)
- [Andy Matuschak — Evergreen Notes (콜드메모리의 Obsidian 버전)](https://notes.andymatuschak.org/Evergreen_notes)

## 강의 메모 후보 (Pain/숫자/삽질/훅)

- **Pain**: "Claude Code 설치는 했는데 뭘 기억하게 해야 할지 모르겠어요"라는 질문이 실제로 가장 많이 들어오는 질문입니다. 도구보다 콘텐츠 설계가 어렵다는 사실을 사람들이 뒤늦게 깨닫습니다.
- **숫자**: 씨앗 메모리 작성 권장 분량 — 레포당 아티클 5~15개, 아티클당 5~10줄이면 충분. 이 이하면 너무 적고, 이 이상이면 노이즈가 커집니다.
- **삽질**: "처음에 모든 대화를 다 캡처하게 설정했더니 한 달 뒤 위키가 200개 아티클이 됐고, Claude가 SessionStart마다 엄청난 토큰을 소비하기 시작했습니다. 다시 처음부터 큐레이션해야 했습니다."
- **훅**: *"도구를 설치하는 데 2시간을 썼는데, 정작 '무엇을 기억하게 할 것인가'에는 5분도 안 썼다면 — 그게 바로 메모리 시스템이 실패하는 이유입니다."*
